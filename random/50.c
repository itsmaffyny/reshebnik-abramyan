#include <stdio.h>
#include <stdbool.h>
 
//CAN U VOTE?
//CAN U SMOKE?
//CAN U DRINK
typedef enum rules{
      VOTEAGE,
      SMOKEAGE,
      DRINKAGE
}RULES;
int main (void)
{
int YourAge;
printf("---CAN YOU VOTE?---\n");
printf("What is your age? ");
scanf("%d", &YourAge);

bool compare = YourAge> enum Rules VOTEAGE; 
                         || enum Rules VOTEAGE;
if (compare == 1){printf("Yes");}
     else{printf("No");}
return 0;
}
